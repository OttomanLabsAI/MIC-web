{
  "name": "mic-web",
  "version": "1.0.0",
  "private": true,
  "description": "Muslims In Construction events site — Box Hill Trek and MIC Gala 2026, served as Cloudflare Workers static assets",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
